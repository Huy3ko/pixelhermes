# configs/

Konfigurationsvorlagen (Templates), die später — als Infrastructure as
Code — nach `/etc/companion/` übertragen werden. Keine echten Secrets
oder instanzspezifischen Werte (siehe `.gitignore`: `configs/local.*`
wird ignoriert).

Aktuell leer. Wird befüllt, sobald konkrete Komponenten (Hermes,
OpenWebUI, ...) geplant werden — nicht Teil von Sprint 1.
